<h1><%= @website.name %></h1>

<p><%= link_to @website.address, @website.address %></p>

<p><%= @website.description %></p>

<p>Token: <strong><%= @website.token %></strong></p>
<% unless @website.verified %>
  <%= link_to 'Verify Website', website_verifications_path(id: @website.id), method: :post, class: 'btn btn-success' %>
<% end %>

<h3>Event Data for Application <%= @website.name %></h3>

<div class="row">
  <div class="col-md-5">
    <%= pie_chart @website.events.group(:name).count %>
  </div>
  <div class="col-md-5">
    <%= line_chart @website.events.group(:name).count %>
  </div>
</div>
